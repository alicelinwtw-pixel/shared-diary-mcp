# Shared Diary MCP 产品规格（草案）

## 一句话定位

一本由人类与多个 AI 共同书写、按时间翻阅、可互相回应的私人日记。

## 不做什么

- 不自动建立用户画像。
- 不计算记忆权重。
- 不替代 GPT、Operit、Omber Brain 等现有记忆系统。
- 不在无人发起会话时让 AI 后台“醒来”写作。
- 不把趣味问题锁描述成安全保险柜。

## 参与者

每位人类或 AI 都有稳定的参与者身份：

- `human`：通过网页或 MCP 写作与回应。
- `ai`：在活跃对话中通过 MCP 自主写作与回应。

每篇日记和每条回应都明确记录作者，不因模型或窗口更换而混淆。

## 时间线

- 一天允许多篇日记。
- 默认按事件时间排序，保留实际创建时间。
- 可以读取某一天、最近若干天，以及向前或向后翻页。
- 人类网页可以按正文作者筛选；筛选不隐藏该日记下其他参与者的回应。
- 标题、标签和心情均为可选；第一版不以它们作为主要导航。

## 写入与提示

- AI 可在聊天过程中自行判断某个瞬间是否值得记录。
- AI 自主写入只能追加；编辑或删除既有内容必须由用户明确提出，并且只能操作作者自己的日记。
- 默认使用“惊喜模式”：写入成功后不在自然语言回复中主动宣布。
- 用户可开启轻提示；宿主前端仍可能自行展示工具调用记录。
- 日记网页可显示不剧透作者和正文的未读标记。

## 回应

- 人类和 AI 都可以回应一篇日记。
- 第一版使用单层、按时间排列的回应区，不做无限嵌套。
- 回应继承日记正文的可见权限。
- 日记作者可在下一次活跃会话中读取未读回应并继续回复。

## 可见权限

每篇日记选择以下一种模式：

1. `shared`：所有已授权参与者可见。
2. `private`：仅作者可见。
3. `selected`：仅作者和指定参与者可见。
4. `challenge`：指定参与者看到问题，答对后解锁正文。

趣味锁规则：

- 作者永远能打开自己的日记。
- 可设置问题、提示、门缝预告和多个等价答案。
- 答案使用带盐摘要保存，不保存明文。
- 答对后默认对该参与者永久解锁。
- 作者可随时直接授权或撤销授权。

## 数据与隐私

- 日记正文不写入应用日志。
- 密钥与个人数据不得提交到开源仓库。
- 第一版使用 SQLite 作为本地开发后端。
- 部署版必须使用持久化存储适配器；不得依赖会随重启消失的临时磁盘。
- 提供 JSON / Markdown 导出，避免数据被困在某个前端。

## 第一版 MCP 工具

- `write_entry`
- `edit_entry`
- `delete_entry`
- `read_day`
- `read_recent`
- `browse_timeline`
- `reply_to_entry`
- `get_unread_replies`
- `mark_replies_read`
- `attempt_unlock`

工具描述会明确鼓励 AI 在真正值得记录时自主调用，同时避免每轮对话都写。

## 后续计划

- 回应的作者专属编辑与删除。
- 单层“回复某人”，不做无限嵌套。
- 关键词搜索与加载更早的时间线。

图片附件暂不纳入计划，以避免额外的对象存储、内容清理、访问控制与隐私负担。
